'use client';
import {useEffect,useState} from 'react';
export default function Prayer(){const [d,setD]=useState<any>(),[coords,setCoords]=useState<[number,number]>([30.0444,31.2357]),[city,setCity]=useState('القاهرة'); const load=(lat:number,lon:number)=>fetch(`/api/prayer?lat=${lat}&lon=${lon}&method=5`).then(r=>r.json()).then(setD); useEffect(()=>{load(...coords)},[]); const locate=()=>navigator.geolocation?.getCurrentPosition(p=>{setCoords([p.coords.latitude,p.coords.longitude]);setCity('موقعي الحالي');load(p.coords.latitude,p.coords.longitude)}); const ps=[['الفجر','Fajr'],['الشروق','Sunrise'],['الظهر','Dhuhr'],['العصر','Asr'],['المغرب','Maghrib'],['العشاء','Isha']];return <div className="page"><div className="wrap"><div className="page-head"><div><h1>🕌 مواقيت الصلاة</h1><p className="muted">{city} · تتحدث تلقائيًا من خدمة مواقيت الصلاة.</p></div><button className="btn" onClick={locate}>استخدم موقعي</button></div><div className="prayers six">{ps.map(([a,b])=><div className="pray" key={a}><span>{a}</span><b>{d?.timings?.[b]||'--:--'}</b></div>)}</div><div className="grid" style={{marginTop:18}}><div className="card"><h3>التاريخ الهجري</h3><p className="arabic big-date">{d?.date?.hijri?.day||'—'} {d?.date?.hijri?.month?.ar||''} {d?.date?.hijri?.year||''}</p></div><div className="card"><h3>طريقة الحساب</h3><p className="muted">الهيئة المصرية للمساحة (Method 5).</p></div><div className="card"><h3>الخطوة القادمة</h3><p className="muted">في نسخة الهاتف نضيف إشعارات الأذان ومؤقت الصلاة القادمة.</p></div></div></div></div>}
